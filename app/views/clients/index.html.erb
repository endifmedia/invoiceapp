<p id="notice"><%= notice %></p>

<!--<h1>All Clients <span><%= link_to 'ADD NEW', new_client_path, class: 'button small' %></span></h1>-->


<div class="row">
  <div class="col-10"><h1>All Clients</h1></div>
  <div class="col-2"><%= link_to 'ADD NEW', new_client_path, class: 'button small' %></div>
</div>
<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Contact</th>
      <th>Email</th>
      <th>Phone</th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= link_to client.company, edit_client_path(client) %></td>
        <td><%= client.contact_name %></td>
        <td><%= client.email %></td>
        <td><%= client.phone %></td>
        <!--<td><%= client.cell %></td>
        <td><%= client.website %></td>
        <td><%= client.address %></td>
        <td><%= client.address_2 %></td>
        <td><%= client.city %></td>
        <td><%= client.state %></td>
        <td><%= client.zip %></td>-->
        <!--<td><%= link_to 'Show', client %></td>
        <td><%= link_to 'Edit', edit_client_path(client) %></td>-->
        <td><%= link_to '', client, method: :delete, class: 'fa fa-minus-circle', data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client', new_client_path %>
